import React from 'react';


export default function Container({ name, width, height, depth, weight, maxWeight, quantity, onDelete = () => { } }) {

    return (
        <div>
            {/* input fields */}
            {/* delete icon */}
        </div>
    );

}